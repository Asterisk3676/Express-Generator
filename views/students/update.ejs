<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/bootstrap.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-5"><%= title %></h1>
        <% if (error) { %>
            <div class="text-danger"> <%= error %> </div>
        <% } %>
        <form action='/students/update' method='post'>
            <input type='hidden' name='students[id]' value='<%= student[0].id %>'/>
            <h6 class="mb-3">
                <p>st_id</p>
                <input type='text' name='students[st_id]' placeholder='รหัสนักศึกษา' value='<%= student[0].st_id %>' />
            </h6>
            <h6 class="mb-3">
                <p>name</p>
                <input type='text' name='students[name]' placeholder='ชื่อนักศึกษา' value='<%= student[0].name %>'/>
            </h6>
            <p>
                <a href="/students" class="btn btn-secondary me-3">Back</a>
                <input type='submit' value='Update' class="btn btn-success"/>
            </p>
        </form>
    </div>
</body>
</html>